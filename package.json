{
  "name": "metiquo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=24.0.0 <25",
    "pnpm": ">=11.0.0 <12"
  },
  "scripts": {
    "build:web": "pnpm --filter @metiquo/web build",
    "contracts:check": "pnpm run contracts:generate && git diff --exit-code -- packages/contracts/src/generated",
    "contracts:generate": "pnpm --filter @metiquo/contracts generate",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint --max-warnings=0 .",
    "spellcheck": "cspell --no-progress --show-suggestions README.md docs/**/*.md docs/**/*.yaml",
    "test:components": "pnpm --filter @metiquo/ui --filter @metiquo/web test",
    "typecheck": "pnpm --recursive --if-present run typecheck",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@cspell/dict-fr-fr": "2.3.2",
    "@playwright/test": "1.62.1",
    "@types/node": "24.13.3",
    "cspell": "10.2.2",
    "eslint": "10.9.1",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.69.0"
  }
}
