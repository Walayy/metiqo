[project]
name = "metiquo-api"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["metiquo-core", "fastapi>=0.116,<1", "uvicorn>=0.35,<1", "email-validator>=2.3,<3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
metiquo-core = { workspace = true }
