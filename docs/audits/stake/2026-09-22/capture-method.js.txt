async function(name) { const startedAt=new Date().toISOString(); const data=await stakeTab.playwright.evaluate(()=>{const clean=x=>{try{const u=new URL(x,location.href);return u.origin+u.pathname}catch{return x}}; const all=[...document.querySelectorAll('*')], ids=new Map(all.map((e,i)=>[e,i])); const nodes=all.filter(e=>!['SCRIPT','STYLE','PATH','DEFS','NOSCRIPT'].includes(e.tagName)).map(e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e),attrs={};for(const a of e.attributes)if(/^(id|class|role|title|alt|type|name|disabled|tabindex|aria-.+|data-.+|href|src)$/.test(a.name))attrs[a.name]=/token|secret|session|nonce|auth|key/i.test(a.name)?'[redacted]':/^(href|src)$/.test(a.name)?clean(a.value):a.value;return {i:ids.get(e),parent:ids.get(e.parentElement)??null,tag:e.tagName.toLowerCase(),attrs,text:[...e.childNodes].filter(n=>n.nodeType===3).map(n=>n.textContent).join('').trim(),rect:{x:r.x,y:r.y,width:r.width,height:r.height},style:{display:s.display,visibility:s.visibility,position:s.position,overflowX:s.overflowX,overflowY:s.overflowY,gridTemplateColumns:s.gridTemplateColumns,gap:s.gap,color:s.color,background:s.backgroundColor,fontSize:s.fontSize,fontWeight:s.fontWeight},rendered:!!(r.width&&r.height&&s.visibility!=='hidden'&&s.display!=='none')};});return {url:clean(location.href),title:document.title,lang:document.documentElement.lang,viewport:{width:innerWidth,height:innerHeight,dpr:devicePixelRatio},totalElements:all.length,expectedNodes:nodes.length,nodeChunks:Array.from({length:Math.ceil(nodes.length/500)},(_,i)=>nodes.slice(i*500,(i+1)*500)),text:document.body.innerText,scripts:[...document.querySelectorAll('script[src]')].map(e=>clean(e.getAttribute('src'))),stylesheets:[...document.querySelectorAll('link[rel=stylesheet]')].map(e=>clean(e.getAttribute('href'))),frames:[...document.querySelectorAll('iframe')].map(e=>({id:e.id,title:e.title,src:clean(e.getAttribute('src')||'')}))};}); data.nodes=data.nodeChunks.flat();delete data.nodeChunks;if(data.nodes.length!==data.expectedNodes)throw new Error('Incomplete DOM transfer');const observedAt=new Date().toISOString();const snapshot=await stakeTab.playwright.domSnapshot();const screenshot=await stakeTab.screenshot({fullPage:false});const files=[];for(const [suffix,bytes] of [['dom.json.gz',auditZlib.gzipSync(JSON.stringify(data))],['ax.txt.gz',auditZlib.gzipSync(snapshot)],['png',screenshot]]){const file=name+'.'+suffix;await auditFs.writeFile(auditDir+'/'+file,bytes);files.push({path:file,bytes:bytes.length,sha256:auditCrypto.createHash('sha256').update(bytes).digest('hex')});}const meta={name,startedAt,observedAt,url:data.url,title:data.title,viewport:data.viewport,totalElements:data.totalElements,nodes:data.nodes.length,completeNodeTransfer:true,files};auditEvidence.push(meta);await auditFs.writeFile(auditDir+'/manifest.json',JSON.stringify({method:'Codex browser read-only DOM; script/style contents, SVG paths and cookie/session storage not exported; URLs without query strings. Captures 002 and 003 contain only the first 2000 exported nodes; later captures verify full chunked transfer.',captures:auditEvidence},null,2));nodeRepl.write({name,observedAt,url:data.url,viewport:data.viewport,nodes:data.nodes.length,bytes:files.reduce((s,f)=>s+f.bytes,0)});return data;}